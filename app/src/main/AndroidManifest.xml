<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestionRecettes"
        tools:targetApi="31">
        <activity
            android:name=".presentation.activities.login_activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.activities.activity_recipe">
        </activity>
        <activity android:name=".presentation.activities.profile_activity" />
        <activity android:name=".presentation.activities.home_activity" />
        <activity android:name=".presentation.activities.signup_activity" />
        <activity android:name=".presentation.activities.add_recipe_activity" />
        <activity android:name=".presentation.activities.edit_profile_activity" />
        <activity android:name=".presentation.activities.edit_recipe_activity" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>